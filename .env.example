# CYBEORCH — copy to .env (do not commit)

# Admin panel base (production subdomain). Leave empty on localhost to use SITE_URL.
# ADMIN_URL=https://admin.cybeorch.com

# SMTP
EMAIL_USER=info@cybeorch.com
EMAIL_PASS=Tushar@10
SMTP_HOST=smtp.stackmail.com
SMTP_PORT=465
SMTP_SECURE=ssl
SMTP_FROM_NAME=CYBEORCH LAB
# Optional: CYBEORCH_SMTP_DEBUG=1 logs SMTP to logs/mail.log
